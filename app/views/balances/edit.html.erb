<header class="simple">
  <h1>Editar Saldos</h1>
</header>

<%= render 'balances/form', form_url: balance_path(@balance.date) %>

<section class="actions">
  <%= link_to 'Cancelar', balance_path(@balance.date) %>
</section>
