<%= render 'balances/report' if @report.valid? %>

<section class="actions">
  <%= link_to 'Inicio', root_path %>
  <%= link_to 'Histórico', balances_path %>
  <%= link_to 'Editar', edit_balance_path(@report.date) %>
</section>
