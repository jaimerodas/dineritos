<header class="simple">
  <section class="header-details">
    <span></span>
    Historial de
  </section>
  <h1>Todas las cuentas</h1>
</header>

<table>
  <tbody>
    <% @report.totals.each do |total| %>
    <%= render 'balances/balance_row',
      name: pretty_date(total.date),
      amount: total.amount,
      diff: total.diff,
      date: total.date
    %>
    <% end %>
  </tbody>
</table>
