<!DOCTYPE html>
<html>
  <head>
    <title>Dineritos</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=5, viewport-fit=cover">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <article>
      <%= yield %>
    </article>
  </body>
</html>
